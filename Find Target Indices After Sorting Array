class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        b = sorted(nums)
        a = []
        for i in range(len(b)):
            if b[i]==target:
                a.append(i)
        return a        
        

class Solution:
    def sumOfTheDigitsOfHarshadNumber(self, x: int) -> int:
        o = x
        s = 0
        while x>0:
            s+=x%10
            x//=10
           
        if o%s==0:
            return s
        else:
            return -1   

class Solution:
    def leftRightDifference(self, nums: List[int]) -> List[int]:
        leftsum=0
        right=sum(nums)
        ans=[]
        for i in nums:
            right-=i
            ans.append(abs(leftsum-right))
            leftsum+=i
            
        return ans 
